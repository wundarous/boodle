<p id="notice"><%= notice %></p>

<h1>Listing Boodles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @boodles.each do |boodle| %>  
      <tr>
        <td><%= boodle.user.user_type.emoji %></td>
        <td><%= boodle.emojis %></td>
        <td><%= boodle.description %></td>
        <td><%= link_to boodle.note, url_for(boodle) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Boodle', new_boodle_path %>
